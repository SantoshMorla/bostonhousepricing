name: Deploy to Vertex AI

on:
  push:
    branches:
      - test

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials: ${{ secrets.GCP_SA_KEY }}
          project_id: genuine-quasar-425514-f6

      - name: Build and push Docker image
        run: |
          # Build your Docker image
          docker build -t model-image .
          docker images
          docker tag model-image us-docker.pkg.dev/genuine-quasar-425514-f6/demo-project/model-img
          docker images
          docker push us-docker.pkg.dev/genuine-quasar-425514-f6/demo-project/model-img
          #ls
         # docker info | grep "Storage Driver"

      
      #- name: check dir
       # run: |
        #  ls
         # sudo cd /var/lib/docker/overlay2
          #ls
          
          
          # Push to GCR
          # docker push gcr.io/genuine-quasar-425514-f6/demo-project/your-model-image:latest
    #  - name: Deploy to Vertex AI
     #   run: |        
      #    gcloud ai endpoints deploy my-model-endpoint \
       #     --image-uri gcr.io/genuine-quasar-425514-f6/your-model-image:latest \
        #    --region us-central1
         #   us-central1-docker.pkg.dev/genuine-quasar-425514-f6/demo-project
# Update vertex.yaml · SantoshMorla/bostonhousepricing@3b4c2cf
